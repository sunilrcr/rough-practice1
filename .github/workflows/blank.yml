name: Hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "demo build"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test Phase
        run: echo "running test"
  deploy:
     runs-on: ubuntu-latest
     needs: test
     steps:
       - name: Deploy the app
         run: echo "deploying the app" 
     

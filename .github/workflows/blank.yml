name: Hello world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "Hello, ${{ vars.my_name}}"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test Phase
        run: echo "running test"
  deploy:
     runs-on: ubuntu-latest
     needs: test
     steps:
       - name: Deploy the app
         run: echo "deploying the app" 
     
